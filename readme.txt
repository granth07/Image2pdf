Image to PDF Converter is a Python app that converts multiple images into a single PDF. With a simple tkinter GUI, you can select images and save them as a PDF in your chosen location. The app uses the Pillow library for processing, making it easy to combine and save images in high quality.
For the provided code to work, you need to install the following Python libraries:


# Pillow: This library is used for opening, manipulating, and saving image files. It also handles the conversion of images to PDF.

# tkinter: This is the standard Python library used for creating the graphical user interface (GUI). It's included with Python, so you 



Just copy the code and run it
